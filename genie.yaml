coffee:
  targets:
    - node

sky:
  s3: 
    - domain: drn:domain/modules/dashkite/com
      # public: true
      website:
        index: index.html
        error: index.html
  yaml:
    targets:
      node:
        - preset: js
          glob: "src/**/*.yaml"
